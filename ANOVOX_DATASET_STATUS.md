# AnoVox数据集状态报告

## 当前下载的数据集

**数据集名称**: AnoVox_Normality_Mono_Town03  
**下载链接**: https://zenodo.org/records/10887912/files/AnoVox_Normality_Mono_Town03.tar.gz  
**文件大小**: 7.1 GB  
**下载状态**: ✅ 已完成  
**解压状态**: ✅ 已完成  

## 数据集内容分析

### ✅ 包含的数据
- **LiDAR点云**: `.npy` 格式，形状 (N, 4)，包含 xyz 坐标和 intensity
- **点云数量**: 大量点云文件（按Scenario组织）

### ❌ 缺少的数据
1. **RGB图像**: 代码期望 `.jpg` 或 `.png` 图像文件
2. **异常掩码**: 代码期望 `.png` 格式的异常标注
3. **相机标定**: 代码期望 `.txt` 或 `.json` 格式的相机内参和外参
4. **训练/测试分割**: 没有明确的 `train/` 和 `test/` 目录结构

### ⚠️ 格式不匹配
- **点云格式**: 代码期望 `.bin` 格式，但数据集是 `.npy` 格式
- **目录结构**: 代码期望标准结构，但数据集是按Scenario组织的
- **数据性质**: 这是"Normality"（常态）数据，可能不包含异常样本

## 数据集结构

```
AnoVox_Normality_Mono_Town03/
└── Scenario_486bab15-e643-418f-9c8b-4a1e8a954f6f/
    └── LIDAR(0, 0, 1.8)(0, 0, 0)_6343367489698313030/
        └── *.npy (点云文件)
```

**期望的结构**:
```
anovox/
├── train/
│   ├── images/          # .jpg或.png
│   ├── pointclouds/     # .bin格式
│   ├── anomaly_masks/   # .png异常掩码
│   └── calibrations/    # .txt或.json相机标定
└── test/
    └── (同上)
```

## 问题分析

### 主要问题

1. **只有常态数据**: "Normality"数据集通常只包含正常样本，无法用于异常检测训练
2. **缺少关键数据**: 没有图像、异常掩码和相机标定
3. **格式不匹配**: 点云格式和目录结构与代码期望不同

### 对融合训练的影响

根据项目规则，阶段四需要：
- **AnoVox训练集**: 用于训练融合头
- **异常掩码**: 作为Ground Truth进行监督学习
- **相机标定**: 用于3D-2D投影

**当前数据集无法满足这些要求**。

## 解决方案

### 方案1：寻找包含异常数据的完整版本（强烈推荐）

在Zenodo上查找：
- 标题包含 "Anomaly"、"Abnormal"、"Dynamic" 的版本
- 描述中明确提到包含异常样本和完整标注的版本
- 包含图像、点云、掩码和标定的完整数据集

**推荐版本**:
- `AnoVox_Dynamic_Mono_Town10_3` (ID: 10869904) - 36.19 GB
- `AnoVox_Static_Mono_Town10_1` (ID: 10881577) - 可能包含完整数据

### 方案2：修改代码适配当前数据集（不推荐）

如果必须使用当前数据集，需要：
1. 修改 `src/data/anovox_dataset.py` 支持 `.npy` 格式
2. 适配Scenario目录结构
3. 处理缺少图像和掩码的情况（创建占位符）
4. **但无法进行异常检测训练**（只有常态数据）

### 方案3：使用其他数据集

如果AnoVox数据集不完整，可以考虑：
- 使用nuScenes数据集（但需要异常标注）
- 使用其他异常检测数据集

## 建议

1. **优先寻找包含异常数据的完整版本**
2. **确认数据集包含**:
   - ✅ RGB图像
   - ✅ 点云数据
   - ✅ 异常掩码（Ground Truth）
   - ✅ 相机标定（内参+外参）
   - ✅ 训练/测试分割

3. **下载完整版本后**:
   ```bash
   # 验证数据集
   python scripts/verify_anovox_dataset.py --data-root /root/autodl-tmp/dataset/anovox
   ```

## 当前状态

- ✅ 数据集已下载到 `/root/autodl-tmp/dataset/anovox/`
- ✅ 数据集已解压
- ❌ 数据集格式不匹配代码期望
- ❌ 缺少关键数据（图像、掩码、标定）
- ⚠️ **无法用于异常检测训练**（只有常态数据）

## 下一步操作

1. **检查Zenodo上的其他版本**，寻找包含异常数据的完整版本
2. **如果找到合适的版本**，下载并验证
3. **如果只有当前数据集**，需要修改代码适配，但无法进行异常检测训练

---

**更新时间**: 2025-11-18  
**状态**: 数据集已下载，但格式不匹配且缺少关键数据





